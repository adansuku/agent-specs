# Tareas: AGEX-010

- [x] 1. Characterization snapshot — extraer el bloque `## Guardrail ... ## Precondición` de cada comando (`sdd-spec.md`, `sdd-plan.md`, `sdd-do.md`, `sdd-review.md`, `sdd-wrap.md`) a `/tmp/agex-010-snapshot/guardrails-before.md`. Extraer los mensajes `⛔ Guardrail: ...` literales a `/tmp/agex-010-snapshot/error-messages-before.txt`. Conservar para diff post-refactor.
- [x] 2. Crear los 5 archivos de `.spec/guardrails/` — `branch-pattern.md`, `proposal-exists.md`, `plan-and-tasks-exist.md`, `all-tasks-done.md`, `review-approved.md`. Contenido: copia literal de los pasos y mensajes del snapshot, reorganizados por tipo de precondición. Cada archivo autoexplicativo con su propio mensaje de error y comando de recuperación.
- [x] 3. Reescribir el bloque `## Guardrail` en los 5 comandos — sustituir el bloque extenso por sección corta que referencia los archivos de `.spec/guardrails/` aplicables en orden, con los parámetros del comando (tipo esperado, quick-fix sí/no, nombre del artefacto). Preservar el tono imperativo ("**Ejecuta esto antes de cualquier otro paso.**") en la nueva sección.
- [x] 4. Verificación final — ejecutar los 5 greps del criterio de éxito (`ls .spec/guardrails/`, `grep ⛔`, `grep "Para aquí"`), diff textual contra snapshot (los mensajes de error deben aparecer literalmente en `.spec/guardrails/`), smoke install en tmpdir con `diff -r` entre fuente y destino. Limpiar `/tmp/agex-010-snapshot/`.
